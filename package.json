{
  "name": "exported-assets",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx rimraf dist && mkdir -p dist && node ./node_modules/copyfiles/copyfiles.js -a *.html *.css *.js *.png *.jpg *.jpeg *.gif *.svg dist && node ./node_modules/copyfiles/copyfiles.js -a css/** js/** img/** images/** dist",
    "start": "npx serve dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rimraf": "^5.0.0",
    "copyfiles": "^2.4.1",
    "serve": "^14.2.0"
  }
}
